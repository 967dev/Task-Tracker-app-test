# 🚫 Настройка БЕЗ Google Cloud Console

Этот метод использует **Google Apps Script** для работы с таблицей. Это проще, быстрее и не требует настройки платежных аккаунтов или сложных API ключей.

## Шаг 1: Настройка Google Таблицы

1.  Создайте новую Google Таблицу на [sheets.google.com](https://sheets.google.com).
2.  Перейдите в меню: **Расширения** (Extensions) > **Apps Script**.
3.  Откроется редактор кода. Удалите весь код в файле `Code.gs`.
4.  Скопируйте код из файла `APPS_SCRIPT.js` (в этом проекте) и вставьте его в редактор.
5.  Нажмите иконку 💾 (Сохранить).

## Шаг 2: Публикация скрипта

1.  В редакторе скриптов нажмите синюю кнопку **Начать развертывание** (Deploy) > **Новое развертывание** (New deployment).
2.  Нажмите на шестеренку ⚙️ рядом с "Выберите тип" и выберите **Веб-приложение** (Web app).
3.  Заполните поля:
    *   **Описание**: `v1`
    *   **Выполнять как**: `От моего имени` (Me) — *Это важно!*
    *   **У кого есть доступ**: `Все` (Anyone) — *Это КРИТИЧЕСКИ важно для работы API!*
4.  Нажмите **Начать развертывание** (Deploy).
5.  Google попросит предоставить доступ ("Разрешить доступ").
    *   Выберите свой аккаунт.
    *   Если появится экран "Google hasn’t verified this app" (Приложение не проверено), нажмите **Advanced** (Дополнительно) и затем **Go to ... (unsafe)** (Перейти на страницу...). *Это нормально, так как это ваш личный скрипт.*
    *   Нажмите **Allow** (Разрешить).
6.  Скопируйте полученный **URL веб-приложения** (Web App URL). Он выглядит как `https://script.google.com/macros/s/.../exec`.

## Шаг 3: Настройка проекта

1.  Откройте файл `.env` в корне проекта.
2.  Закомментируйте старые настройки Sheets.
3.  Добавьте новую переменную с вашим URL:

```env
# Старое (можно удалить или закомментировать)
# GOOGLE_SHEETS_SPREADSHEET_ID=...
# GOOGLE_SHEETS_CREDENTIALS=...

# Новое
GOOGLE_APPS_SCRIPT_URL=https://script.google.com/macros/s/ВАШ_ДЛИННЫЙ_ID/exec
PORT=3001
```

## Шаг 4: Запуск

Теперь нам нужно запускать другой файл сервера.

1.  Откройте `package.json`.
2.  Найдите строку `"server": "nodemon server/index.js"`.
3.  Замените на: `"server": "nodemon server/index-gas.js"`.

Или запускайте вручную:

```bash
node server/index-gas.js
```

## Как это работает?

Вместо того чтобы Node.js подключался к базе данных Google напрямую, он отправляет запрос вашему скрипту. Скрипт, находясь "внутри" Google, имеет прямой доступ к таблице и делает всю работу.

### Преимущества:
*   ✅ Не нужен Google Cloud Console.
*   ✅ Не нужен Service Account JSON.
*   ✅ Таблица остается приватной (доступ есть только у вас и у скрипта).

### Важно знать:
*   У скриптов есть квоты (около 20,000 запросов в день), чего с головой хватит для Mini App.
*   При изменении кода в Apps Script нужно создавать **Новое развертывание** (Deploy > Manage deployments > Edit > New version), чтобы изменения вступили в силу.
