# 🎯 Bidask Advent Calendar - Telegram Mini App

Геймифицированный адвент-календарь с заданиями внутри Telegram.

## 📋 Особенности

- ✅ **Бесплатный хостинг**: Работает на Vercel (Frontend + Serverless API)
- 📊 **Бесплатная БД**: Использует Google Sheets через Google Apps Script (без сложных настроек Google Cloud)
- 🎨 **Премиум Дизайн**: Параллакс-эффект, снегопад, glassmorphism
- 📱 **Telegram Native**: Полная интеграция с WebApp API
- ⚡ **Быстро**: React + Vite

## 🛠 Технологии

- **Frontend**: React.js + Vite
- **Backend**: Vercel Serverless Functions (`api/index.js`) -> Google Apps Script
- **Database**: Google Sheets
- **Интеграция**: Telegram Web Apps API

## 🚀 Быстрый старт

### 1. Подготовка
Установите зависимости:
```bash
npm install
```

### 2. Настройка базы данных (Google Sheets)
Вам **не нужно** настраивать сложные Service Accounts или Google Cloud Console. Мы используем Google Apps Script как "мост".

1.  Создайте новую Google Таблицу.
2.  Перейдите в **Расширения > Apps Script**.
3.  Вставьте код из файла `APPS_SCRIPT.js` (есть в корне проекта).
4.  Нажмите **Начать развертывание > Новое развертывание**.
    *   Тип: **Веб-приложение**
    *   У кого есть доступ: **Все (Anyone)**  <-- ВАЖНО!
5.  Скопируйте полученный URL (`.../exec`).

### 3. Подключение к проекту
Откройте файл `api/index.js` и вставьте ваш URL скрипта:

```javascript
const GAS_URL = 'https://script.google.com/macros/s/ВАШ_ДЛИННЫЙ_ИД/exec';
```

### 4. Запуск локально
```bash
npm run dev
```
Откроет приложение на `http://localhost:5173`.
*Примечание: В браузере (вне Telegram) будет использоваться тестовый пользователь `Dev User`.*

## 🌍 Деплой (Запуск в интернет)

Проект полностью настроен для **Vercel**.

1. Загрузите код на GitHub.
2. Зарегистрируйтесь на [Vercel.com](https://vercel.com).
3. Нажмите **Add New -> Project** и выберите ваш репозиторий.
4. Нажмите **Deploy**.
5. Полученную ссылку вставьте в BotFather (`/mybots` -> Bot Settings -> Menu Button).

Подробнее см. в [DEPLOY.md](DEPLOY.md).

## 📊 Как работает проверка заданий?

1.  **Пользователь** отправляет ссылку-отчет через приложение.
2.  Ссылка появляется в вашей Google Таблице.
3.  Статус задания в приложении меняется на 🟡 **На проверке**.
4.  **Админ** проверяет ссылку.
5.  Если всё ок — Админ пишет в ячейку слово `TRUE`, `true`, `COMPLETED` или ставит `1`.
6.  Статус задания у пользователя меняется на 🟢 **Выполнено**.

## 📝 Структура проекта

```
bidask-advent/
├── api/             # Бэкенд для Vercel (прокси к Google)
├── src/
│   ├── components/  # React компоненты (Background, TaskModal...)
│   ├── assets/      # Картинки
│   └── App.jsx      # Главная логика
├── server/          # (Устарело) Локальный сервер
├── APPS_SCRIPT.js   # Код для Google Таблиц
└── vercel.json      # Настройка маршрутизации Vercel
```

## 🤝 Поддержка
Если приложение не работает:
1. Проверьте лог Vercel (Functions).
2. Убедитесь, что в Google Apps Script доступ открыт для **"Всех" (Anyone)**.
3. Убедитесь, что вы сделали **Новое развертывание** после изменения кода скрипта.
