# 🎯 Quest Tracker - Telegram Mini App

Геймифицированное приложение для выполнения заданий внутри Telegram.

## 📋 Возможности

- ✅ Автоматическая авторизация через Telegram WebApp API
- 📊 Интеграция с Google Sheets для хранения данных
- 🎨 Современный дизайн с glassmorphism эффектами
- 📱 Адаптивный интерфейс для мобильных устройств
- ⚡ Быстрая работа благодаря React + Vite
- 🔄 Автоматическое обновление статусов заданий

## 🛠 Технологии

- **Frontend**: React.js + Vite
- **Backend**: Node.js + Express
- **Database**: Google Sheets API
- **Integration**: Telegram Web Apps API

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка Google Sheets

#### Шаг 1: Создайте Google Таблицу

1. Перейдите на [Google Sheets](https://sheets.google.com)
2. Создайте новую таблицу
3. Скопируйте ID таблицы из URL (находится между `/d/` и `/edit`)
   - Пример: `https://docs.google.com/spreadsheets/d/YOUR_SPREADSHEET_ID/edit`

#### Шаг 2: Создайте Service Account

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com)
2. Создайте новый проект или выберите существующий
3. Включите Google Sheets API:
   - Перейдите в "APIs & Services" > "Library"
   - Найдите "Google Sheets API"
   - Нажмите "Enable"
4. Создайте Service Account:
   - Перейдите в "APIs & Services" > "Credentials"
   - Нажмите "Create Credentials" > "Service Account"
   - Заполните детали и нажмите "Create"
   - Пропустите опциональные шаги
5. Создайте ключ:
   - Откройте созданный Service Account
   - Перейдите на вкладку "Keys"
   - Нажмите "Add Key" > "Create new key"
   - Выберите JSON формат
   - Ключ будет скачан на ваш компьютер

#### Шаг 3: Предоставьте доступ к таблице

1. Откройте скачанный JSON файл с ключом
2. Найдите поле `client_email`
3. Откройте вашу Google Таблицу
4. Нажмите "Share" (Поделиться)
5. Добавьте email из `client_email` с правами "Editor"

### 3. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```bash
cp .env.example .env
```

Отредактируйте `.env` файл:

```env
# Google Sheets Configuration
GOOGLE_SHEETS_SPREADSHEET_ID=ваш_spreadsheet_id
GOOGLE_SHEETS_CREDENTIALS={"type":"service_account","project_id":"...","private_key":"..."}

# Server Configuration
PORT=3001
```

**Важно**: В `GOOGLE_SHEETS_CREDENTIALS` вставьте весь содержимое скачанного JSON файла **в одну строку**.

### 4. Запуск приложения

#### Режим разработки

```bash
npm run dev
```

Это запустит:
- Frontend на `http://localhost:5173`
- Backend на `http://localhost:3001`

#### Для production

```bash
npm run build
```

## 🔧 Настройка Telegram Bot

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Используйте команду `/newbot` и следуйте инструкциям
3. После создания бота, используйте `/newapp` для создания Mini App
4. Укажите URL вашего приложения (для разработки можно использовать ngrok)

### Использование ngrok для разработки

```bash
ngrok http 5173
```

Скопируйте HTTPS URL и используйте его в настройках Mini App.

## 📊 Структура Google Таблицы

Приложение автоматически создаст следующие столбцы:

| User ID | Username | Задание 1 (Тест) | Дата регистрации |
|---------|----------|------------------|------------------|
| 123456  | @alex_step | https://t.me/... | 17.12.2025 |

## 🎮 Добавление новых заданий

Отредактируйте файл `src/App.jsx`:

```javascript
const TASKS = [
  {
    id: 1,
    title: 'Название задания',
    emoji: '💬',
    description: 'Описание задания...',
    taskLink: 'https://t.me/your_link',
    instructions: [
      'Шаг 1',
      'Шаг 2',
      'Шаг 3'
    ]
  },
  // Добавьте новое задание
  {
    id: 2,
    title: 'Второе задание',
    emoji: '🎯',
    // ...
  }
]
```

И добавьте соответствующий столбец в `server/index.js`:

```javascript
headerValues: ['User ID', 'Username', 'Задание 1 (Тест)', 'Задание 2', 'Дата регистрации']
```

## 🔒 Безопасность

- Приложение использует `window.Telegram.WebApp.initDataUnsafe` для получения данных пользователя
- Данные защищены протоколами Telegram
- Невозможно подделать user_id или username
- Все запросы валидируются на backend

## 📱 Особенности Telegram Mini App

- Приложение автоматически расширяется на весь экран
- Использует нативные темы Telegram
- Поддерживает открытие ссылок через `openTelegramLink()`
- Адаптировано под мобильные устройства

## 🐛 Отладка

### Проверка подключения к Google Sheets

```bash
curl http://localhost:3001/api/health
```

### Просмотр логов сервера

Сервер выводит подробные логи о:
- Регистрации пользователей
- Отправке заданий
- Ошибках подключения к Google Sheets

### Тестирование в Telegram

1. Откройте бота в Telegram
2. Используйте команду для открытия Mini App
3. Проверьте консоль браузера (если доступно)

## 📝 Структура проекта

```
quest-tracker/
├── src/
│   ├── components/
│   │   ├── TaskCard.jsx       # Карточка задания
│   │   ├── TaskModal.jsx      # Модальное окно задания
│   │   └── UserInfo.jsx       # Информация о пользователе
│   ├── App.jsx                # Главный компонент
│   ├── main.jsx               # Точка входа React
│   └── index.css              # Стили приложения
├── server/
│   └── index.js               # Express сервер
├── index.html                 # HTML шаблон
├── vite.config.js             # Конфигурация Vite
├── package.json               # Зависимости
└── .env                       # Переменные окружения
```

## 🤝 Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте логи сервера
2. Убедитесь, что Google Sheets API настроен правильно
3. Проверьте, что Service Account имеет доступ к таблице
4. Убедитесь, что все переменные окружения установлены

## 📄 Лицензия

MIT
