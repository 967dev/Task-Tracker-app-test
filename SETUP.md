# ⚙️ Подробная настройка (Setup Guide)

Это приложение использует упрощенную архитектуру без сложных настроек серверов и баз данных. Бэкендом служит Vercel, базой данных — обычная Google Таблица.

## Шаг 1: Google Таблица (База данных)

Вам **не нужны** API ключи Google Cloud или Service Accounts!

1.  Создайте пустую **Google Таблицу**.
    *   Лист можно назвать как угодно (по умолчанию `Лист1`).
2.  В верхнем меню выберите **Расширения (Extensions) > Apps Script**.
3.  Откроется редактор кода. Удалите всё, что там есть.
4.  Скопируйте код из файла `APPS_SCRIPT.js` (находится в корне этого проекта).
5.  Вставьте код в редактор Google.
6.  Нажмите иконку 💾 (Сохранить).

## Шаг 2: Публикация API (Deploy)

Это самый важный шаг. Без него приложение не увидит таблицу.

1.  В редакторе скриптов справа сверху нажмите синюю кнопку **Начать развертывание (Deploy)**.
2.  Выберите **Новое развертывание (New deployment)**.
3.  Нажмите на шестеренку (тип) и выберите **Веб-приложение (Web app)**.
4.  Заполните поля:
    *   **Описание**: `v1` (любое).
    *   **Выполнять как (Execute as)**: `Меня (Me)` (ваш email).
    *   **У кого есть доступ (Who has access)**: ⚠️ Выберите **Все (Anyone)**.
        *   *Если вы выберете "Только я" или "Google аккаунт", приложение не заработает!*
5.  Нажмите **Начать (Deploy)**.
6.  Вам выдадут **Веб-приложение URL** (Web App URL). Он выглядит как `https://script.google.com/macros/s/.../exec`.
7.  **Скопируйте этот URL.**

## Шаг 3: Подключение к Frontend

1.  Откройте файл `api/index.js` в папке проекта на компьютере.
2.  Найдите строку 14:
    ```javascript
    const GAS_URL = '...';
    ```
3.  Вставьте туда ваш URL из шага 2.

## Шаг 4: Развертывание в интернет (Vercel)

1.  Установите Git и Node.js, если нет.
2.  В папке проекта выполните команды:
    ```bash
    git init
    git add .
    git commit -m "Initial commit"
    ```
3.  Создайте репозиторий на GitHub и загрузите туда код.
4.  Зайдите на [Vercel](https://vercel.com).
5.  Импортируйте проект с GitHub.
6.  Нажмите **Deploy**.

## ❌ Решение частых проблем

### "У меня в таблице ничего не появляется"
*   Проверьте, что в настройках Deploy (Apps Script) доступ стоит для **Anyone**.
*   Попробуйте открыть ссылку `.../exec` в режиме инкогнито. Если видите JSON `{"error": "..."}`, то доступ есть. Если просит войти в Google — доступа нет.

### "Я изменил код в таблице, но ничего не поменялось"
*   Google Apps Script запоминает версию кода при деплое. Если вы поменяли код, нужно снова нажать **Deploy > New deployment** и получить **новую ссылку** (или обновить старую версию, но проще новую).

### "Статус задания не меняется на Выполнено"
*   Чтобы подтвердить задание, нужно в ячейке таблицы написать **TRUE** (или `true`, `completed`, `ok`, `1`). Просто текста недостаточно.
