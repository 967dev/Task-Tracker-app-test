# 🚀 Быстрая настройка Quest Tracker

## 1️⃣ Настройка Google Sheets

### Создание таблицы
1. Откройте [Google Sheets](https://sheets.google.com)
2. Создайте новую таблицу
3. Скопируйте ID из URL: `https://docs.google.com/spreadsheets/d/ВАШ_ID/edit`

### Создание Service Account
1. Перейдите в [Google Cloud Console](https://console.cloud.google.com)
2. Создайте проект или выберите существующий
3. Включите **Google Sheets API**:
   - APIs & Services → Library → Google Sheets API → Enable
4. Создайте Service Account:
   - APIs & Services → Credentials → Create Credentials → Service Account
5. Создайте JSON ключ:
   - Откройте Service Account → Keys → Add Key → Create new key → JSON

### Предоставление доступа
1. Откройте скачанный JSON файл
2. Скопируйте значение `client_email`
3. В Google Таблице нажмите Share
4. Добавьте этот email с правами **Editor**

## 2️⃣ Настройка .env файла

Создайте файл `.env` в корне проекта:

```env
GOOGLE_SHEETS_SPREADSHEET_ID=ваш_id_таблицы
GOOGLE_SHEETS_CREDENTIALS={"type":"service_account",...весь JSON в одну строку...}
PORT=3001
```

**Важно**: JSON должен быть в одну строку без переносов!

## 3️⃣ Настройка Telegram Bot

### Создание бота
1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Сохраните токен бота

### Создание Mini App
1. В BotFather отправьте `/newapp`
2. Выберите своего бота
3. Заполните информацию о приложении
4. Укажите URL приложения

### Для локальной разработки (ngrok)

```bash
# Установите ngrok
# Запустите туннель
ngrok http 5173

# Скопируйте HTTPS URL и используйте в настройках Mini App
```

## 4️⃣ Запуск приложения

```bash
# Установка зависимостей (уже выполнено)
npm install

# Режим разработки (запускает frontend + backend)
npm run dev

# Frontend: http://localhost:5173
# Backend: http://localhost:3001
```

## 5️⃣ Проверка работы

### Проверка backend
```bash
curl http://localhost:3001/api/health
```

Ожидаемый ответ:
```json
{"status":"ok","googleSheets":"connected"}
```

### Проверка в Telegram
1. Откройте бота
2. Запустите Mini App
3. Проверьте отображение данных
4. Попробуйте отправить задание

## ❗ Решение проблем

### "Google Sheets not initialized"
- Проверьте `.env` файл
- Убедитесь, что JSON в одну строку
- Проверьте, что Service Account имеет доступ к таблице

### "User not found"
- Убедитесь, что у пользователя есть username в Telegram
- Проверьте, что данные сохраняются в таблице

### Приложение не открывается в Telegram
- Проверьте URL в настройках Mini App
- Убедитесь, что используете HTTPS (через ngrok)
- Проверьте, что приложение запущено

## 📝 Дополнительные команды

```bash
# Только frontend
npm run client

# Только backend
npm run server

# Production build
npm run build
```

## 🎯 Что дальше?

1. Добавьте больше заданий в `src/App.jsx`
2. Настройте дизайн под свой бренд
3. Добавьте новые столбцы в таблицу
4. Настройте систему вознаграждений
