# 🎮 Как добавлять новые задания

Система заданий состоит из двух частей:
1.  **Frontend (`src/App.jsx`)**: Внешний вид и текст задания.
2.  **Backend (`APPS_SCRIPT.js`)**: Логика проверки и сохранения статуса.

Если вы хотите добавить новое задание (например, ID=12), нужно выполнить оба шага.

## Шаг 1: Добавление в интерфейс (Frontend)

Откройте файл `src/App.jsx` и найдите массив `TASKS`. Добавьте новый объект:

```javascript
{
    id: 12, // Уникальный номер!
    title: 'Моё новое задание',
    emoji: '🔥', // Иконка
    description: 'Короткое описание под заголовком',
    
    // Ссылка кнопки "Выполнить"
    taskLink: 'https://t.me/channel',
    
    // (Опционально) Текст для копирования (паста)
    pasta: '#BidaskAdvent',
    
    // (Опционально) Подзадачи (чек-лист в модалке)
    instructions: [
        'Подпишись на канал',
        'Поставь лайк',
        'Отправь скриншот'
    ],
    
    // (Опционально) Если true, можно отправлять любые ссылки, не только t.me
    skipTelegramValidation: false 
},
```

## Шаг 2: Регистрация в Базе Данных (Backend)

Google Apps Script должен знать, что появилось новое задание, чтобы создать для него колонку.

1.  Откройте файл `APPS_SCRIPT.js` (или ваш скрипт в редакторе Google).
2.  В самом верху найдите строчку:
    ```javascript
    const TASK_IDS = [1, 2, 3, 4, 6, 7, 8, 9, 10, 11, 16];
    ```
3.  Добавьте туда ваш новый ID:
    ```javascript
    const TASK_IDS = [1, 2, ..., 11, 16, 12]; // <-- добавили 12
    ```
4.  **Важно:** Сделайте **New Deployment** (Новое развертывание) в Google Apps Script и обновите ссылку в `api/index.js`, если она изменилась (обычно меняется при новом деплое).

*Примечание: При следующем запуске скрипт автоматически добавит новую колонку "Задание 12" в вашу Google Таблицу.*
